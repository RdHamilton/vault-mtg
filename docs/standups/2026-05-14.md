# Daily Standup — 2026-05-14

## CI Status
UNKNOWN — `gh` CLI and GitHub Actions API not queryable via available MCP tools. Last commit on `main` is `6a6d6fc` (PR #1921, 2026-05-13T06:18:08Z). No failure signals visible in commit messages or PR state. **Manual check recommended**: verify the staging-deploy and ci workflows passed on `6a6d6fc` before any release cut.

## Shipped Yesterday (2026-05-13)
- #1921 fix(infra): auto-recover from dirty migration state before migrate up — RdHamilton
- #1920 fix(migration): 000077 — remove DROP INDEX, let DROP COLUMN cascade — RdHamilton
- #1919 fix: quests 500 (missing account_id column) + match history SSE refresh — RdHamilton
- #1918 fix(frontend): remove daemon gates, route pages to BFF — RdHamilton

> Note: #1917 (collection BFF + CI fixes) and #1916 (daemon dev env var) merged 2026-05-12 — also part of yesterday's sprint.

## In Flight
| PR | Description | Author | Days open |
|---|---|---|---|
| — | No open PRs | — | — |

## Board Snapshot (Project #30 — v0.4.0)
| Status | Count |
|---|---|
| Todo | N/A — project board not queryable via available MCP tools |
| In Progress | N/A |
| PR Review | N/A |
| Done | N/A |

> Ray action: run `gh project item-list 30 --owner RdHamilton --format json --limit 50` to get counts. Based on merged PR volume, a significant batch of Done items landed yesterday (6 PRs).

## Blocked / Needs Attention
- No issues labeled `blocked` found.
- **Staging migration incident (resolved yesterday)**: Migration 000077 left the DB dirty at version 77. PRs #1919 + #1920 + #1921 fix the migration content and add automatic dirty-state recovery to the deploy script. Confirm staging is healthy post-deploy.
- **CI queryability gap**: `gh run list` and GitHub Actions workflow-run MCP tools are unavailable in this session. CI status above is inferred, not verified.

## Ray Action Items
- Verify staging is healthy: `GET staging-api.vaultmtg.app/api/v1/quests/active` should return 200.
- Confirm migration 000077 applied cleanly (`quests.account_id` is now `BIGINT`).
- Run `gh project item-list 30 --owner RdHamilton --format json --limit 50` to get board snapshot counts for next standup.
- Review CI run on `6a6d6fc` in GitHub Actions to confirm staging-deploy and ci workflows are green.

## Agent Reports Due Today
Today is **Thursday 2026-05-14** — not a Monday, so no weekly BA/CS/Growth reports are scheduled.

| Report | Schedule | Last seen in docs/ | Status |
|---|---|---|---|
| Finance P&L | Monthly | `docs/finance/2026-04-pl.md` (generated today) | ✅ Present |
| BA weekly metrics | Mondays | `docs/reports/2026-05-beta-cost-model.md` (2026-05-11) | Not due |
| CS feedback summary | Mondays | `docs/feedback/nps-survey.md` (2026-05-11) | Not due |
| Growth/marketing | Mondays | Not present | Not due |
