# Daily Standup — 2026-05-19

## CI Status
**UNKNOWN** — no GitHub Actions workflow-run API available via current MCP tooling.
Inferred **GREEN** based on 4 clean PR merges today (each requiring Ray + Sarah / Lee approvals). Confirm manually if a release is imminent.

> Note: `release.yml` will fail at the SSM preflight gate until Phase 5b is complete — the `/vaultmtg/production/*` parameters do not yet exist in AWS. **Do not cut a production `v*` tag until Phase 5b lands.**

## Shipped in Last 24 Hours

**Today (2026-05-19)**
- #2389 `fix(ci): add ssm: and kms: to security-review-gate CATCH_ALL_STRINGS` — Ray (12:49 UTC)
- #2379 `feat(ci): S-07 security-review-gate CI workflow (vault-mtg)` — Ray (09:15 UTC)
- #2380 `feat(test): R-17 SMOKE-4 Playwright staging rebuild smoke gate` — Ray (09:11 UTC)
- #2372 `fix: rollback.yml BFF service name via restart-bff.sh` — Ray (08:14 UTC)

**Yesterday (2026-05-18)**
- #2273 `fix(frontend): update daemon-download repo refs to vault-mtg` — Ray (20:46 UTC)
- #2271 `fix: remove Phase-3 interim go.mod replace directives` — Ray (20:25 UTC)
- #2267 `chore: post-rename CI smoke (#1739)` — Ray (19:40 UTC)
- #2260 `refactor: Go module-path rename → github.com/RdHamilton/vault-mtg/` — Ray (13:39 UTC)
- #2259 `fix: migrate SSM paths /mtga-companion/* → /vaultmtg/*` — Ray (13:23 UTC)
- #2258 `fix: update GOPRIVATE/GONOSUMDB and repo refs in CI workflows` — Ray (13:14 UTC)

**10 PRs merged in the last 24 hours.** ADR-022 VaultMTG rename Phases 1–4 are now complete. Phase 5 (SSM provisioning, staging CORS, release unblock) is next.

## In Flight

| PR | Description | Author | Days Open |
|---|---|---|---|
| #2265 | `ci(deps): bump actions/upload-artifact 4→7` | dependabot | 1 |
| #2264 | `ci(deps): bump dorny/paths-filter 3→4` | dependabot | 1 |
| #2263 | `ci(deps): bump goreleaser/goreleaser-action 6→7` | dependabot | 1 |
| #2257 | `docs(standup): daily standup 2026-05-18` | RdHamilton | 1 — **unmerged** |

All 3 Dependabot PRs are **major-version bumps**. Per CLAUDE.md, each requires a changelog review for breaking changes before merging.

## Board Snapshot (Project #30 — v0.4.0)
Project board API not available via current MCP tooling — snapshot omitted. Check manually at GitHub Projects.

| Status | Count |
|---|---|
| Todo | — |
| In Progress | — |
| PR Review | — |
| Done | — |

## Blocked / Needs Attention

1. **Release freeze** — `release.yml` will fail until Phase 5b creates `/vaultmtg/production/*` SSM parameters and `vaultmtg-deploy-artifacts-staging` S3 bucket. No production release possible until this lands.
2. **Staging CORS misconfiguration** — R-17 smoke tests (PR #2380) found that `staging-api.vaultmtg.app` BFF does not have `stg-app.vaultmtg.app` in `ALLOWED_ORIGINS`. Tests 3 & 4 of the smoke suite fail. Bob needs to update the `ALLOWED_ORIGINS` SSM parameter on staging.
3. **Staging TLS cert mismatch** — Staging BFF presents the production cert (`api.vaultmtg.app`). Currently worked around with `ignoreHTTPSErrors: true` in the Playwright config. Needs a proper staging cert.
4. **Yesterday's standup PR #2257 is open** — not merged to main.
5. **Finance May P&L missing** — `docs/finance/` only has `2026-04-pl.md`. May report is ~19 days overdue.
6. **BA / CS / Growth weekly reports not found** — `docs/reports/` and `docs/feedback/` contain no Monday weekly reports (only `2026-05-beta-cost-model.md` and `nps-survey.md`).

## Ray Action Items

1. **Phase 5b (critical path to release)** — create/mirror `/vaultmtg/production/*` and `/vaultmtg/staging/*` SSM parameters; provision `vaultmtg-deploy-artifacts-staging` S3 bucket. Until done, no production release is possible.
2. **Staging CORS fix** — confirm with Bob that `ALLOWED_ORIGINS` SSM on staging includes `stg-app.vaultmtg.app`. Block on Phase 5b or track as separate infra ticket.
3. **Review Dependabot major bumps** (#2263, #2264, #2265) — read changelogs, check for breaking-change call sites per CLAUDE.md dependency policy; merge or close.
4. **Close or merge standup PR #2257** — leftover from yesterday.
5. **Trigger Finance agent** — May P&L is 19 days overdue; invoke `finance-controller` agent to produce `docs/finance/2026-05-pl.md`.

## Agent Reports Due Today

Today is **Monday 2026-05-19** — weekly agent reports should have run.

| Agent | Report | Status |
|---|---|---|
| `business-analyst` | Weekly metrics summary | **NOT FOUND** in `docs/reports/` |
| `customer-success` | Weekly feedback summary | **NOT FOUND** in `docs/feedback/` |
| `growth-marketing` | Weekly campaign/SEO report | **NOT FOUND** |
| `finance-controller` | May P&L (monthly, due 2026-05-01) | **NOT FOUND** — 19 days overdue |
