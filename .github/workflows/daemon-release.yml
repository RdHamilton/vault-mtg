# daemon-release.yml — Release workflow for the daemon module
#
# Responsibility: build, package, sign, and publish a GitHub Release via GoReleaser.
# Triggers on `daemon/v*` tags (e.g. daemon/v1.2.3) or manual workflow_dispatch.
#
# CI (tests on push/PR) is handled exclusively by daemon.yml.
#
# Job graph:
#   goreleaser   — runs on ubuntu-latest; tests, cross-compiles all targets, builds
#                  Windows NSIS installer (via makensis), creates GitHub Release draft.
#   sign-macos   — runs on macos-latest; downloads darwin universal binary, code-signs,
#                  notarizes, wraps in .pkg + .dmg, uploads signed artifacts to the
#                  existing GitHub Release. Tag-guarded to avoid burning Apple quota.

name: Daemon Release

on:
  push:
    tags:
      - 'daemon/v*'
  # Manual dispatch is supported for dry-runs and debugging.
  # NOTE: without a daemon/v* tag, GoReleaser must be invoked with --snapshot
  # (already the case when there is no matching tag).  workflow_dispatch on a
  # branch (not a tag) will fail at the GoReleaser step unless you add
  # --snapshot to the args in the goreleaser step below.
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  goreleaser:
    name: GoReleaser (build + release)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # GoReleaser needs full git history for changelog

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Configure private module access
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global url."https://${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"

      - name: Install makensis (Windows NSIS installer builder)
        run: sudo apt-get update -qq && sudo apt-get install -y nsis

      - name: Download Go dependencies
        working-directory: services/daemon
        env:
          GONOSUMDB: github.com/RdHamilton/MTGA-Companion
          GOPRIVATE: github.com/RdHamilton/MTGA-Companion
          GOFLAGS: -mod=mod
        run: go mod download

      - name: Run tests (pre-release gate)
        working-directory: services/daemon
        env:
          GONOSUMDB: github.com/RdHamilton/MTGA-Companion
          GOPRIVATE: github.com/RdHamilton/MTGA-Companion
        run: go test -race ./...

      - name: Resolve semver tag for GoReleaser
        # GoReleaser v2 requires the GORELEASER_CURRENT_TAG value to exist as
        # an actual git tag (it validates tag-to-commit alignment). Our mono-repo
        # uses 'daemon/v*' tags for namespacing, which are not valid semver.
        #
        # Fix: strip the 'daemon/' prefix to get a plain semver string, then
        # create a local-only git tag with that name pointing at the same commit.
        # The local tag satisfies GoReleaser's validation without pushing a
        # duplicate tag to the remote.
        id: semver
        run: |
          RAW="${{ github.ref_name }}"
          SEMVER="${RAW#daemon/}"
          echo "tag=${SEMVER}" >> "$GITHUB_OUTPUT"
          # Create a local alias tag so GoReleaser's tag-to-commit validation passes.
          git tag "${SEMVER}"

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: '~> v2'
          args: release --config services/daemon/.goreleaser.yml --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GONOSUMDB: github.com/RdHamilton/MTGA-Companion
          GOPRIVATE: github.com/RdHamilton/MTGA-Companion
          GITHUB_WORKSPACE: ${{ github.workspace }}
          # Override the version GoReleaser reads from git so it sees the plain
          # semver (e.g. 'v0.3.1-rc1') instead of the prefixed tag ('daemon/v0.3.1-rc1').
          GORELEASER_CURRENT_TAG: ${{ steps.semver.outputs.tag }}

      - name: Upload darwin universal binary for sign-macos
        # Share the unsigned darwin binary so sign-macos can sign and notarize it.
        # GoReleaser v2 with format:binary writes the universal binary to a
        # subdirectory named after the universal_binaries id + _darwin_all, so
        # the path is dist/daemon-universal_darwin_all/vaultmtg-daemon.
        uses: actions/upload-artifact@v4
        with:
          name: vaultmtg-daemon-darwin-universal
          path: dist/daemon-universal_darwin_all/vaultmtg-daemon
          if-no-files-found: error

  sign-macos:
    name: Sign & Notarize macOS universal binary
    runs-on: macos-latest
    needs: [goreleaser]
    # Guard: only run when triggered by a real daemon/v* tag.
    # workflow_dispatch without a tag must not burn Apple notarization quota
    # or waste macOS runner minutes. (#1658)
    if: startsWith(github.ref, 'refs/tags/daemon/v')
    timeout-minutes: 30

    env:
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      # The artifact uploaded by goreleaser contains the file 'vaultmtg-daemon'
      # (the binary name set in .goreleaser.yml builds[].binary). The artifact
      # container is named 'vaultmtg-daemon-darwin-universal' but the file inside
      # is 'vaultmtg-daemon'. BINARY must match the file name, not the artifact name.
      BINARY: vaultmtg-daemon
      PKG: vaultmtg-daemon-darwin-universal.pkg
      DMG: vaultmtg-daemon-darwin-universal.dmg

    steps:
      - name: Download darwin universal binary artifact
        uses: actions/download-artifact@v4
        with:
          name: vaultmtg-daemon-darwin-universal
          path: bin/

      - name: Import Developer ID Application certificate
        env:
          CERT_BASE64: ${{ secrets.APPLE_DEVELOPER_CERTIFICATE }}
          CERT_PASSWORD: ${{ secrets.APPLE_DEVELOPER_CERTIFICATE_PASSWORD }}
        run: |
          echo "$CERT_BASE64" | base64 --decode > /tmp/dev-id-app.p12
          security create-keychain -p "temp-keychain-password" build.keychain
          security default-keychain -s build.keychain
          security list-keychains -d user -s build.keychain login.keychain
          security unlock-keychain -p "temp-keychain-password" build.keychain
          security set-keychain-settings -t 7200 -l build.keychain
          security import /tmp/dev-id-app.p12 \
            -k build.keychain \
            -P "$CERT_PASSWORD" \
            -T /usr/bin/codesign \
            -T /usr/bin/pkgbuild
          security set-key-partition-list \
            -S apple-tool:,apple:,codesign: \
            -s -k "temp-keychain-password" build.keychain
          rm /tmp/dev-id-app.p12

      - name: Import Developer ID Installer certificate
        env:
          CERT_BASE64: ${{ secrets.APPLE_INSTALLER_CERTIFICATE }}
          CERT_PASSWORD: ${{ secrets.APPLE_INSTALLER_CERTIFICATE_PASSWORD }}
        run: |
          echo "$CERT_BASE64" | base64 --decode > /tmp/dev-id-installer.p12
          security import /tmp/dev-id-installer.p12 \
            -k build.keychain \
            -P "$CERT_PASSWORD" \
            -T /usr/bin/pkgbuild \
            -T /usr/bin/productbuild
          # Include pkgbuild: in partition list so pkgbuild can access the installer key
          security set-key-partition-list \
            -S apple-tool:,apple:,codesign:,pkgbuild: \
            -s -k "temp-keychain-password" build.keychain
          rm /tmp/dev-id-installer.p12

      - name: Sign macOS binary
        env:
          SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          chmod +x bin/${{ env.BINARY }}
          codesign \
            --force \
            --deep \
            --options runtime \
            --sign "$SIGNING_IDENTITY" \
            --timestamp \
            bin/${{ env.BINARY }}
          codesign --verify --verbose bin/${{ env.BINARY }}

      - name: Create .pkg installer
        env:
          INSTALLER_IDENTITY: ${{ secrets.APPLE_INSTALLER_IDENTITY }}
        run: |
          mkdir -p pkg-root/usr/local/bin
          cp bin/${{ env.BINARY }} pkg-root/usr/local/bin/vaultmtg-daemon
          pkgbuild \
            --root pkg-root \
            --identifier com.vaultmtg.daemon \
            --version "${GITHUB_REF#refs/tags/daemon/v}" \
            --install-location / \
            --sign "$INSTALLER_IDENTITY" \
            --timestamp \
            ${{ env.PKG }}

      - name: Wrap .pkg in .dmg
        run: |
          mkdir -p dmg-staging
          cp ${{ env.PKG }} dmg-staging/
          hdiutil create \
            -volname "MTGA Companion Daemon" \
            -srcfolder dmg-staging \
            -ov \
            -format UDZO \
            ${{ env.DMG }}

      - name: Notarize .dmg
        env:
          APPLE_NOTARIZATION_PASSWORD: ${{ secrets.APPLE_NOTARIZATION_PASSWORD }}
        run: |
          xcrun notarytool submit ${{ env.DMG }} \
            --apple-id "${{ secrets.APPLE_NOTARIZATION_APPLE_ID }}" \
            --team-id "${{ env.APPLE_TEAM_ID }}" \
            --password "$APPLE_NOTARIZATION_PASSWORD" \
            --wait

      - name: Staple notarization ticket
        run: xcrun stapler staple ${{ env.DMG }}

      - name: Clean up temporary keychain
        if: always()
        run: |
          security delete-keychain build.keychain || true

      - name: Upload signed macOS artifacts to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          gh release upload "$TAG" \
            "${{ env.PKG }}" \
            "${{ env.DMG }}" \
            --repo "${{ github.repository }}" \
            --clobber
