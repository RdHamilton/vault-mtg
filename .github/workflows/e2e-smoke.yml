name: E2E Smoke Tests

on:
  workflow_dispatch:
  # Called by release.yml as the Tier 3 release gate
  workflow_call:

permissions:
  contents: read

jobs:
  e2e-smoke:
    name: E2E Smoke Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: mtga
          POSTGRES_PASSWORD: mtga
          POSTGRES_DB: mtga_e2e
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.25'
        cache-dependency-path: go.sum

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      run: |
        cd frontend
        npm ci

    - name: Install Playwright browsers
      run: |
        cd frontend
        npx playwright install chromium --with-deps

    - name: Build API server
      env:
        GONOSUMDB: github.com/RdHamilton/MTGA-Companion
        GOPRIVATE: github.com/RdHamilton/MTGA-Companion
      run: |
        cd services/bff
        go build -o ../../bin/mtga-bff ./cmd/main.go

    - name: Run E2E smoke tests
      run: |
        cd frontend
        npm run test:e2e:smoke
      env:
        CI: true
        DATABASE_URL: postgres://mtga:mtga@localhost:5432/mtga_e2e?sslmode=disable
        # BFF config.Load() defaults MTGA_ENV to "production" which requires
        # CLERK_SECRET_KEY. Set to "development" so the BFF starts without a
        # Clerk secret key -- smoke tests validate UI connectivity, not auth.
        MTGA_ENV: development

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: e2e-smoke-results
        path: |
          frontend/playwright-report/
          frontend/test-results/
        retention-days: 7
