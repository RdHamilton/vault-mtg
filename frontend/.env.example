# BFF base URL (including /api/v1 path prefix).
# Set this in the Vercel dashboard per environment (Production + Preview).
# For production (S3+CloudFront), set VITE_BFF_URL=https://api.vaultmtg.app/api/v1 at build time.
# Leave unset for local development — the client defaults to http://localhost:8080/api/v1.
# See docs/adr/006-vercel-bff-connectivity.md and docs/adr/ADR-008-frontend-serving-model.md.
VITE_BFF_URL=https://api.vaultmtg.app/api/v1

# Clerk publishable key — required for auth UI (ADR-009).
# Get your key from https://dashboard.clerk.com → API Keys.
# Use pk_test_... for development, pk_live_... for production.
#
# Social OAuth providers (Google, Facebook, Apple) are enabled in the Clerk Dashboard
# under Social connections — no additional frontend code required.
# Dashboard: https://dashboard.clerk.com → Social connections
# Docs: https://clerk.com/docs/authentication/social-connections/overview
VITE_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_KEY_HERE

# PostHog analytics — optional in local dev, required in production.
# Leave VITE_POSTHOG_KEY unset (or empty) to disable all analytics calls (safe for local dev and tests).
# Get your key from https://app.posthog.com → Project Settings → API Keys.
# Default host is https://app.posthog.com; override to use EU cloud or a self-hosted instance.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://app.posthog.com

# Sentry DSN for error monitoring.
# Leave empty (or unset) in local dev and test — Sentry.init() is skipped when this is absent.
# Set to the project DSN from https://sentry.io → Project Settings → Client Keys.
VITE_SENTRY_DSN=
