{
  "//": "VaultMTG SPA stylelint config — #340: color-no-hex promoted from warn to hard-fail (error). The tree is clean of non-categorical raw hex (#309 Pass B + #339 + #320 all landed). Categorical MTG-pip / data-viz hex values that have no token equivalent are documented in src/test/noRawHex.test.ts CATEGORICAL_HEX and suppressed via inline /* stylelint-disable-line color-no-hex */ comments. index.css (the token-definition file) is excluded from linting entirely — it is the one authoritative source of raw hex.",
  "rules": {
    "color-no-hex": true
  },
  "//ignoreFiles": "src/index.css is the single token-definition file — the one place raw hex is allowed (it defines the --vault-* primitives the rest of the app references), so it is excluded from this lint entirely.",
  "ignoreFiles": ["src/index.css", "dist/**", "node_modules/**", "storybook-static/**"]
}
